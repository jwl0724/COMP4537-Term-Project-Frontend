Utils.populateById({
    label: LABEL,
    submit: SUBMIT
});

Utils.populatePlaceholderById({
    email: EMAIL,
    password: PASSWORD,
    confirmation: CONFIRMATION
});

const credentialClass = new Credentials();